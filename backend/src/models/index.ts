export interface GeneratedArticleProps {
    article: string;
    title: string;
    description: string;
    textPromptImage: string;
}

export interface GenericErrorResponse {
    error: boolean;
    message: string;
}
